# NVIDIA NIM free API key — get one free at https://build.nvidia.com (no card required)
NVIDIA_API_KEY=your-nvidia-nim-key-here

# Optional overrides (defaults shown). Swap these to use any OpenAI-compatible provider.
# NIM_BASE_URL=https://integrate.api.nvidia.com/v1
# NIM_MODEL=meta/llama-3.1-8b-instruct
